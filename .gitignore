.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/
.build/
.swiftpm/
.idea/

# Core ML model, kept out of history: 41 MB of weights that git would carry
# forever, and it is reproducible. See ShotDex/Resources/Models/README.md for how
# to put it back. The app works without it — the Sky mask finds nothing when the
# model is missing.
ShotDex/Resources/Models/*.mlpackage/

# Stray build/download artifacts.
big-lama.zip
lama/
__pycache__/
*.pyc
